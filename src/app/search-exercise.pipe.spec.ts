import { SearchExercisePipe } from './search-exercise.pipe';

describe('SearchExercisePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchExercisePipe();
    expect(pipe).toBeTruthy();
  });
});
